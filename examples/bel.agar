message clean_room {
    string task;
    int value;
}

message task_done {
    string status;
}

agent cleaner {
}

agent mother {
    beliefs {
        int x = 0;
        int y = 7;
    }
    /*
    sense {
        int x = 0;
        x = x+1;
    }
    */
    goals {
        g1: 5==6;
        g2: (3<4) || (5==5);
    }
    rules {
        when (
        ) then {
            print("Hello from mother agent!");
        }
        when (
        ) then {
            print("I am mother agent, I can do something!");
        }
    }
}