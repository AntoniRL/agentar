message clean_room {
    string task;
    int value;
}

message task_done {
    string status;

}

agent cleaner {
    beliefs {
        int x = 0;
        int y = 7;
    }
    goals {
        g1: 5==6;
        g2: (3==4) || (5==5);
    }
    rules {
        when (
        ) then {
        }
        when (
        ) then {
        }
    }
}

agent mother {
    initialize {
    }
}