message ping{
    list myList;
}
/*
agent child {
    receive ping {
        when () then {
            list myList = msg.myList;
            myList[] = 10;
            print("I received:", myList);
        }
    }
}
*/
agent mother {
    fields{
        list mmList;
    }
    initialize{
        int i = 5;
        for (int j = 0; j < i; j=j+1) {
            self.mmList[] = j;
            print("Adding to list:", j);
        }
        print(self.mmList);
    }
}