message mes{
    int x;
}

agent child{
    fields{
        int x;
        float y = 5.0;
    }
    initialize {
        print("Hello");
    }
}

agent mother {
    fields{
        //int x;
    }
    initialize {
        agentid agentid_;
        agentid_ = spawn(child, {x: 5, y: 5.5});
        print(agentid_);
    } 
}