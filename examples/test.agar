agent my_agent {
    fields {
        int time = 0;
        float tick_interval = 0.5;
    }
    rules {
        when () then {
            sleep(self.tick_interval);
            self.time = self.time + 1;
        }
    }
}


agent mother{

}