message mes{
    int x;
}
agent mother {
    fields{
        int x;
    }
    initialize {
        dict my_dict; 
        my_dict[(1,2)] = "value";
    }    
    goals {
        g1: 4 == 4;
        g2: 5 < 6;
    }
    rules {
        when (5==4 || 5==5) then {
            print("I am here");
        }
    }
}