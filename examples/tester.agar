message mes{
    int x;
}

agent child{
    fields{
        int x;
        float y = 5.0;
    }
    initialize {
    }
    receive mes{
        when () then {
            print(msg.x);
            int x;
            x = msg.x;
            print(x+3);
            print(msg.x);
        }
    }
}

agent mother {
    fields{
        int x = 5;
    }
    initialize {
        //agentid agentid_;
        //agentid_ = spawn(child, {x: 5, y: 5.5});
        //mes msg_ = mes{x: 1};
        //send(agentid_, msg_, msgType_request);
        print("Hello");
        //int x = 5;
        //do runer(&x);
        //sleep(1);
        //print(x);
    } 
    action runer(pointer<int> x): void {
        print(type(x));
        x = x + 2;
        print(x);
    }
}